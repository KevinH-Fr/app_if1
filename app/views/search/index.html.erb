<h1>Search#index</h1>
<p>Find me in app/views/search/index.html.erb</p>

<h1 class="test-center"> <%= pluralize(@pilotes.count, "Result")%> </h1>
<%= render 'search/form' %>


<table>
  <thead>
    <tr>
      <th><%= sort_link(@q, :nom, "Nom", default_order: :asc) %></th>
      <th><%= sort_link(@q, :statut, "Statut", default_order: :desc) %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pilotes.each do |pilote| %>
      <tr>
        <td><%= pilote.nom %></td>
        <td><%= pilote.statut %></td>
        <td><%= link_to 'Show', pilote %></td>
        <td><%= link_to 'Edit', edit_pilote_path(pilote) %></td>
        <td><%= link_to 'Destroy', pilote, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New pilote', new_pilote_path %>